meta {
  name: Unsupported API version
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v99/students?page=1&size=3
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("unsupported API version fails safely", function() {
    expect([400, 404]).to.include(res.status);
  });
}
