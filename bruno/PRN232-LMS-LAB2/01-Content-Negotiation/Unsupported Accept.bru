meta {
  name: Unsupported Accept
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/students?page=1&size=3
  body: none
  auth: none
}

headers {
  Accept: text/csv
}

tests {
  test("unsupported Accept returns 406", function() {
    expect(res.status).to.equal(406);
  });
}
