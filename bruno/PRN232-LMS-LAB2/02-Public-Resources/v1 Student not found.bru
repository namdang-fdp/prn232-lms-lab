meta {
  name: v1 Student not found
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/students/999999
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("missing student returns 404", function() {
    expect(res.status).to.equal(404);
    expect(res.body).to.have.property("success", false);
  });
}
