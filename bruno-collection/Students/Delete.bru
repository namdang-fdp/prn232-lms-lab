meta {
  name: Delete
  type: http
  seq: 6
}

delete {
  url: http://localhost:8080/api/students/{{studentId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("returns ApiResponse success for delete", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("success", true);
  });
}
