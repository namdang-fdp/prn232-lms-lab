meta {
  name: Get By ID Valid
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/api/subjects/{{subjectId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("returns ApiResponse success with subject", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.have.property("success", true);
    expect(res.body.data.subjectId).to.equal(Number(bru.getVar("subjectId")));
  });
}
