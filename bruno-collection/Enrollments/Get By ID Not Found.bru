meta {
  name: Get By ID Not Found
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/enrollments/99999
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("returns ApiResponse not found", function() {
    expect(res.status).to.equal(404);
    expect(res.body).to.have.property("success", false);
  });
}
